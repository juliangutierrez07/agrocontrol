APP_ENV=local
APP_DEBUG=false
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=agrocontrol
DB_CHARSET=utf8mb4

# Rate limiting para el formulario de login
# Número máximo de intentos fallidos antes del bloqueo
LOGIN_MAX_ATTEMPTS=5
# Duración del bloqueo en segundos (900 = 15 minutos)
LOGIN_LOCKOUT_SECS=900
